// import NoteList from "../NoteList/NoteList";
import css from "./App.module.css";

function App() {
  const onClickDelete = () => {};

  return (
    <div className={css.app}>
      <header className={css.toolbar}>
        {/* <NoteList/ onClick={onClickDelete}> */}
        {/* Компонент SearchBox */}
        {/* Пагінація */}
        {/* Кнопка створення нотатки */}
      </header>
    </div>
  );
}

export default App;
